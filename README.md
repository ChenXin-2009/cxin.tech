# Personal Portfolio Website

一个基于 Next.js + TypeScript 的现代个人作品集静态网站。

## 技术栈

- **Next.js 14** - React 框架，支持 App Router
- **TypeScript** - 类型安全
- **Tailwind CSS** - 实用优先的 CSS 框架
- **MDX** - Markdown 与 React 组件的结合
- **静态生成（SSG）** - 完整的静态网站导出

## 项目特性

✨ 支持 MDX，可在 Markdown 中使用 React 组件
🎨 Tailwind CSS 样式系统
📱 响应式设计
🌓 暗色模式支持
⚡ 性能优化，适合 SEO
🚀 一键部署到任何静态托管服务

## 开始使用

### 1. 安装依赖
```bash
npm install
```

### 2. 开发模式
```bash
npm run dev
```
访问 `http://localhost:3000`

### 3. 生产构建
```bash
npm run build
npm start
```

### 4. 静态导出
```bash
npm run build
npm run export
```
输出文件在 `out/` 目录

## 文件结构

- `src/app/` - 页面和路由
- `src/components/` - 可复用组件
- `src/app/*.mdx` - MDX 格式的页面
- `public/` - 静态资源

## 自定义

### 修改主页
编辑 `src/app/page.tsx`

### 添加新页面
在 `src/app/` 中创建新目录和 `page.tsx` 或 `page.mdx`

### 修改主题
编辑 `src/app/globals.css` 中的 CSS 变量或 `tailwind.config.ts`

### 添加博客文章
在 `src/app/blog/` 中创建新目录，添加 `.mdx` 文件

## 部署建议

- **Vercel** - 最佳选择（Next.js 官方）
- **GitHub Pages** - 部署静态输出
- **Netlify** - 简单配置
- **其他静态托管** - 上传 `out/` 目录

## 许可证

MIT
